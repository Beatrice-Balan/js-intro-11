// string
let name = 'John';
console.log(name);

//number
let age = '45';
console.log(age);

// declare a variable - with no intial value
let city; //JS makes it "undefined" by default, just created
console.log(city);

//assignig value to variable - no longer have to use "let/ const/ var"
city = 'New York'; // do NOT use "let/var/const" again, it will create a NEW variable (container)
console.log(city);

//initialization of a variable = declaration + assignment
let country = 'Usa'; //make variable and assign it a value from the bat, if you have it
console.log(country);